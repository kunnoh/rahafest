import { StyleSheet } from "react-native";

const feedsStyle = StyleSheet.create({
  createPostPressable: {
    position: "absolute",
    bottom: 10,
    right: 35,
    backgroundColor: "purple",
    borderRadius: 20,
    paddingHorizontal: 20,
    paddingVertical: 5,
  },
});

export default feedsStyle;
